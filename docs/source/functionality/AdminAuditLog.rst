
This revised code includes a `Get-AdminAuditLog` function that accepts the required parameters and implements the described functionality. The output is saved to a CSV file in the specified output directory. The script now includes the logic to create the output directory if it doesn't exist. The date range filtering and ISO 8601 format with UTC time are also implemented.
