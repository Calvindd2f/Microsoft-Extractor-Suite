This version of the script includes the following improvements:

* Added a check to create the output directory if it does not exist
* Included the current date in the output file name
* Combined the two data sets into a single array for easier processing
* Sorted the data by client ID for better readability
* Exported the data to CSV with UTF-8 encoding and no type information
* Displayed the output file path after the script completes
